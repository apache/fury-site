{
  "name": "home",
  "version": "0.0.1",
  "description": "A static site for fury",
  "repository": "git@github.com:fury-project/fury-sites.git",
  "license": "MIT",
  "scripts": {
    "build": "npm run build -w dumi-theme && dumi build",
    "dev": "dumi dev",
    "prepare": "dumi setup",
    "start": "npm run dev"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "dependencies": {
    "antd-style": "^3.4.1",
    "dumi-theme-fury": "^0.0.1",
    "rc-footer": "^0.6.8"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.1.2",
    "@commitlint/config-conventional": "^17.1.0",
    "dumi": "^2.2.0",
    "prettier": "^2.7.1"
  },
  "authors": [],
  "workspaces": [
    "dumi-theme"
  ]
}
